시험을 위한 연습으로 이런걸 해보자...

1. 이클립스 다이나믹 웹프로젝트를 만든다 => testProject (contextPath => /test) 						/testProject(default)를 /test로 변경
- web.xml 추가해서 WebContent/WEB-INF/classes

2. 우리가 맨날 쓰는 SERVER (SERVER DB계정)에 테이블을 하나 만든다.
테이블명 : WATER(생수)
컬럼 :
- WATER_NO => PK : 생수번호 (NUMBER)
- BRAND : 브랜드 (VARCHAR2 40)
- PRICE : 가격 (NUMBER)

데이터 추가
1, 삼다수, 1000
2, 백산수, 2000
3, 에비앙, 3000

3. src 아래 com.kh.water 만들고 vo, service, dao, db ... 패키지 만든다

4. Water.java, WaterService.java, WaterDao.java, water-mapper.xml 만들기
+ 서블릿

5. index.jsp => form 태그내에 생수명과 가격을 입력받아서 전송하는 화면.. (submit 버튼)

6. 추가하는 메소드 구현 => db로 확인

7. update.jsp => 수정하는 화면...

8. 수정하는 메소드 구현... => db로 확인

9. ** 혹시 모르니... JDBCTemplate, driver.properties 작성

==> 평가자체크리스트 (Ajax는 제외) (오늘안에 해보자 죽어도 주말까지는 해야함)


---------------------------------------------------------------------------------------------------------

==> 문제해결과 시나리오 4문제																		(ctrl+h > FileSearch > errorPage.jsp)

request.setAttribute => 키(apple)값을 주고 벨류(애플)를 주고 ...									-> set을 안했거나
request.getAttribute(samsung)																-> get을 안했거나 키값의 오탈자(일치여부) 주의

=> errorPage.jsp 참고할 것

내가 넘기려는 값이 있다면
request.setAttribute("b", b);																// doGet에서!!
request.setAttribute("at", at);

이런식으로 넘겨야함

받을 때도
<% Member m = (Member)~~~~ %> 이런거 없으면 널포인트 날 수 있음
